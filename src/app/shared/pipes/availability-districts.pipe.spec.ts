import { AvailabilityDistrictsPipe } from './availability-districts.pipe';

describe('AvailabilityDistrictsPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailabilityDistrictsPipe();
    expect(pipe).toBeTruthy();
  });
});
